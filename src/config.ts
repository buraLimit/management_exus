import { DefaultConfigProps } from 'types/config';
export const APP_DEFAULT_PATH = '/user-list';
export const DRAWER_WIDTH = 260;

const config: DefaultConfigProps = {
  fontFamily: `'Public Sans', sans-serif`,
  i18n: 'en',
  miniDrawer: false,
  container: true
};

export default config;
